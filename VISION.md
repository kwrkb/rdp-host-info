# VISION.md

## Project Vision

Windowsのリモートデスクトップ接続に必要な情報と状態を、ホスト側で簡単に確認できるツールを作る。

本ツールは、Windowsのリモートデスクトップ設定を全面的に自動変更するものではない。

利用者が接続に必要な情報を確認し、現在のPCが接続を受け付けられる状態かを理解できるようにすることを目的とする。

---

## 背景

Windowsのリモートデスクトップは、接続先PCで確認すべき項目が複数の場所に分散している。

代表的な確認項目には、以下がある。

- PC名
- IPアドレス
- Windowsエディション
- リモートデスクトップの有効状態
- 接続に使用するユーザー名
- ネットワークの状態
- ファイアウォールの状態
- スリープ設定
- TailscaleなどのVPN経由の接続先

これらを個別に確認するには、Windows設定、コントロールパネル、PowerShell、コマンドプロンプトなどを横断する必要がある。

その結果、接続する側に何を入力すればよいのか分からない、設定は有効なのに接続できない、ユーザー名の形式が分からない、といった問題が起きやすい。

さらに、実際に接続できない原因は「リモートデスクトップが無効」よりも、以下のような周辺条件にあることが多い。

- Windows Homeでそもそもホストになれない
- ネットワークが「パブリック」判定で、ファイアウォールのRDPルールが現在のプロファイルに適用されていない
- 接続に使うユーザーがRemote Desktop Usersにも管理者にも属していない
- Microsoftアカウント運用で、入力すべきユーザー名の形式が分からない
- PINは設定しているがパスワードでのログインを試していない

本ツールは、これらを含めて「なぜ接続できないのか」を利用者が理解できる状態を作る。

---

## 目的

ホスト側PCでツールを実行するだけで、リモートデスクトップ接続に必要な情報を一覧表示する。

利用者が、次に何を確認すべきか判断できる状態を作る。

---

## 提供する価値

### 接続先情報をまとめて表示する

接続元PCで入力するための情報を、分かりやすい形式で表示する。

表示候補は以下とする。

- PC名
- ローカルIPv4アドレス
- Tailscale IPアドレス
- 接続に使用できるユーザー名（アカウント種別に応じた入力形式）
- 推奨する接続先
- Windowsエディション

### 接続受け入れ状態を確認する

現在のPCが、リモートデスクトップ接続を受け付けられる状態か確認する。

確認候補は以下とする。

- WindowsがRDPホストに対応しているか（Homeエディションではないか）
- リモートデスクトップが有効か
- Remote Desktop Servicesが動作しているか
- ファイアウォールでRDPが、現在のネットワークプロファイルに対して許可されているか
- RDPポートが待ち受け状態か
- 接続対象ユーザーが存在し、RDP接続を許可されたグループに属しているか
- PCがスリープしやすい状態でないか

### 問題を人が理解できる形で説明する

単に成功失敗を表示するのではなく、接続できない可能性がある理由を短く説明する。

例:

```
[NG] Remote Desktop is disabled

Enable Remote Desktop from Windows Settings.
Settings > System > Remote Desktop

[NG] Network is set to Public and Remote Desktop is not allowed

The firewall's Remote Desktop allowance does not apply to the current
network. Change the network to Private.

[WARN] PC sleeps after 15 minutes

Remote Desktop connections may be refused while the PC is asleep.
```

---

## 基本方針

### ホスト側専用ツールとする

本ツールは、接続される側のWindows PCで実行する。

接続元PCの管理や、実際の接続操作は対象外とする。

### 診断と情報表示を中心とする

初期段階では、Windows設定の自動変更を主目的としない。

安全性と予測可能性を優先し、現在の状態を正確に表示する。

### 接続情報をコピー可能にする

表示した情報は、接続元PCへ渡しやすい形式にする。

将来的には以下を検討する。

- クリップボードへのコピー
- テキストファイルへの出力
- ".rdp" ファイルの生成
- QRコード表示

### ローカルネットワークとVPN接続を優先する

利用シーンは以下を想定する。

- 同一LAN内からの接続
- TailscaleなどのVPN経由での接続

インターネットへRDPポートを直接公開する構成は推奨しない。

### Windows標準機能を利用する

可能な限り、Windows標準のAPI、レジストリ情報を利用する。

非公式なRDPホスト化や、Windows Homeでの制限回避は行わない。

---

## 想定ユーザー

- Windowsのリモートデスクトップ設定に慣れていない人
- 家庭内の別PCへ接続したい人
- Tailscale経由で自宅PCへ接続したい人
- 家族や知人のPC設定を支援する人
- 接続先情報を確認するたびにWindows設定を探したくない人

---

## 想定利用フロー

1. ホスト側PCでツールを実行する

```
rdp-host-info
```

2. 接続に必要な情報を確認する

```
Remote Desktop Host Information

PC Name:
  OMEN16

Windows:
  Windows 11 Pro

Connection Address:
  Local IP:      192.168.1.20
  Tailscale IP:  100.80.10.5

Username:
  OMEN16\yugo    (local account)

Recommended:
  100.80.10.5
```

3. 接続可能性を確認する

```
Remote Desktop Status

[OK] Windows supports Remote Desktop hosting
[OK] Remote Desktop is enabled
[OK] Remote Desktop Services is running
[OK] Windows Firewall allows Remote Desktop (Private profile, active)
[OK] TCP 3389 is listening
[OK] User is allowed to connect (Administrators)
[WARN] PC sleeps after 15 minutes
```

4. 表示された情報を接続元PCへ渡す

接続元では、Windows Appまたはリモートデスクトップクライアントに接続先とユーザー名を入力する。

---

## MVP

最初のバージョンでは、以下を実装する。

### 接続情報

- PC名
- Windowsエディション（Home / Pro / Enterprise などを判定できる形で取得する）
- ローカルIPv4アドレス（デフォルトルート側のインターフェースを優先する）
- Tailscale IPアドレス
- 現在のユーザー名
- **RDP接続用ユーザー名形式**
  - アカウント種別を判定し、接続元で入力すべき形式を提示する
    - ローカルアカウント: `PC名\ユーザー名`
    - Microsoftアカウント: `MicrosoftAccount\メールアドレス`
    - Entra ID (Azure AD): `AzureAD\UPN`
    - ドメイン参加: `ドメイン名\ユーザー名`
  - 判定できない場合は、断定せず候補と注意書きを表示する

### 状態確認

- RDPホスト対応エディションか（Homeの場合はその旨を明示する）
- リモートデスクトップが有効か
- Remote Desktop Servicesが起動しているか
- ファイアウォールルールが、**現在アクティブなネットワークプロファイル（Domain / Private / Public）に対して**有効か
- RDPの待ち受けポートが待ち受け状態か（ポート番号は3389固定とせず、設定値を読む）
- 接続対象ユーザーが、RDP接続を許可されたグループに属しているか
- スリープ設定に注意が必要か（best-effort）

### 出力

- 人間向けの標準出力
- コピーしやすい簡潔な接続情報
- 問題がある項目への短い説明

### 出力言語

出力（ヘッダー・Message・Hint・Notes を含む全文言）は既定で英語とし、`-lang ja` で日本語に切り替えられるようにする。

- 既定は英語（`-lang` 未指定 = `en`）
- `-lang ja` で全文言が日本語になる（英語との混在は許容しない）
- `-lang` に `en` / `ja` 以外の値を渡した場合はエラーで終了する（黙って既定言語にフォールバックしない）
- 翻訳しない（言語非依存の識別子として扱う）:
  - ステータスラベル `[OK]` / `[NG]` / `[WARN]` / `[??]`
  - ファイアウォールプロファイル名（`Private` / `Domain+Private` など、Windows 側の名称）
  - IPアドレス、PC名、ユーザー名、`AzureAD\` / `MicrosoftAccount\` プレフィックス
- OSのUI言語からの自動判定は行わない（挙動を`-lang`の指定だけで予測可能にする）

---

## MVPで扱わないもの

初期版では、以下を対象外とする。

- リモートデスクトップ設定の自動変更
- Windowsユーザーの作成
- パスワードの設定
- ファイアウォールルールの変更
- スリープ設定の変更
- ネットワークプロファイルの変更
- ルーターのポート開放
- UPnP設定
- インターネットへのRDP直接公開
- Windows HomeでのRDPホスト化
- RDP Wrapperの導入
- 接続元PCの設定
- 接続資格情報の保存
- 実際のログイン試験

---

## 将来構想

### 接続情報のエクスポート

```
rdp-host-info export
```

以下の形式を検討する。

- プレーンテキスト
- JSON
- Markdown
- ".rdp" ファイル

### GUI

CLIの診断ロジックを再利用し、Windows向けGUIを提供する。

GUIでは以下を重視する。

- 接続先を大きく表示する
- ユーザー名をワンクリックでコピーする
- 問題箇所を赤、注意事項を黄色で表示する
- Windows設定の該当画面を開く

### QRコード表示

Tailscale IPやPC名などをQRコード化し、スマートフォンや別PCへ渡せるようにする。

### 修復支援

自動修復ではなく、利用者が明示的に選択した項目のみ変更できる仕組みを検討する。

ただし、診断と接続情報表示を本ツールの中心に据え続ける。

---

## 技術方針

### 実装言語

Goを第一候補とする。

理由:

- 単一バイナリとして配布しやすい
- Windows向けCLIを作りやすい
- ネットワーク情報の取得と整形に向いている
- 将来的にGUIやJSON出力へ拡張しやすい
- Windows APIとの境界を整理しやすい

### 情報取得の原則

**ロケール依存の出力を文字列パースしない。**

`net localgroup`、`sc query`、`netstat`、`powercfg` などのコマンド出力は、OSの表示言語によって変化する。日本語環境で動作しても、他の言語環境では壊れる。

一次情報源として、以下を優先する。

- レジストリ
- Windows API（`golang.org/x/sys/windows`）
- 既知SID（グループ判定は名前ではなくSIDで行う）
- GUID / 数値（電源設定などは表示ラベルではなく識別子で読む）

外部コマンドやPowerShellの呼び出しは、他に手段がない場合の最終手段とする。起動コストが大きく、出力形式の互換性も保証されない。

### 主な取得手段

- PC名: `GetComputerNameEx`
- エディション / RDP有効状態 / RDPポート番号: レジストリ
- サービス状態: Service Control Manager（**最小権限で開く。全権限を要求すると管理者権限が必要になる**）
- ネットワークインターフェース / IPアドレス: `net` パッケージ
- Tailscale IP: CGNAT範囲（100.64.0.0/10）のアドレスを持つインターフェースを探す（Tailscale CLIへの依存を避ける）
- 待ち受けポート: TCPテーブルAPI
- ファイアウォール / ネットワークプロファイル: Windows Firewall API
- グループ所属: 既知SIDからの解決
- 電源設定: 電源管理API（取得できない場合は黙って省略する）

### 診断ロジックの構造

診断項目ごとに独立したインターフェースとして実装する。

- 取得手段を差し替えても、上位の表示ロジックが影響を受けないようにする
- 実OSに依存しないテストを書けるようにする
- CLIGUIJSON出力で同じ診断ロジックを再利用できるようにする

---

## 設計原則

- 設定を勝手に変更しない
- パスワードや資格情報を取得しない
- 管理者権限なしでも可能な範囲を表示する
- 管理者権限が必要な確認項目は明示する
- 取得できなかった項目は、成功とも失敗とも偽らず「不明」として扱う
- 断定できない情報（ユーザー名形式など）は、断定せず候補として示す
- 結果だけでなく理由を説明する
- Windows標準機能の範囲を尊重する
- RDPのインターネット直接公開を推奨しない
- 出力は人間が読めることを優先する
- 将来のGUIでも同じ診断ロジックを再利用できる構造にする

---

## 成功条件

利用者がツールを実行したあと、次の3点を理解できること。

1. このPCはリモートデスクトップ接続を受け付けられるか
2. 接続元PCに何を入力すればよいか
3. 接続できない場合、どの設定を確認すべきか

---

## プロジェクトの核

このプロジェクトは、Windowsのリモートデスクトップを自動設定するツールではない。

接続先PCの状態と接続情報を一か所に集め、利用者が迷わず次の行動を選べるようにするための、ホスト側診断ツールである。
